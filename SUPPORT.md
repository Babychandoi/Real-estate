# Hỗ trợ

- Lỗi chức năng: tạo ticket nội bộ với URL, thời điểm, mã yêu cầu/case và ảnh đã che PII.
- Sự cố bảo mật hoặc rò rỉ dữ liệu: dùng kênh riêng theo `SECURITY.md`, không gửi PII qua chat công khai.
- Trước khi chuyển ticket, chạy smoke test trong `scripts/smoke-test.ps1` và đính kèm kết quả không chứa secret.

